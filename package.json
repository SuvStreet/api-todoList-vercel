{
  "name": "api-vercel",
  "version": "1.0.0",
  "description": "This is a ready RESTful API created with the JSON Server package",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}